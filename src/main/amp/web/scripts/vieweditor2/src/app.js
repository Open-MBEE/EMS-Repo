// Define application namespace
/*
var app = {
	// Dispatcher to pass events between components of the application
	dispatcher: _.clone(Backbone.Events),
};

// Add callbacks for startup events
$( document ).ready(function() {

});

addEventListener('WebComponentsReady', function() {
	app.dispatcher.trigger("application-start");
});

// Add error reporting
app.dispatcher.on("backend-error datamodel-error" , function(msg) {
	console.log(msg);
});
*/