<webscript>
	<shortname>get project with {projectId}</shortname>
    <description>
    	get project with {projectId}
	</description>
  	<url>/workspaces/{workspaceId}/sites/{siteId}/projects/{projectId}</url>
  	<format default="json">any</format>
  	<authentication>user</authentication>
  	<transaction allow="readonly">required</transaction>
  	<family>MMS REST API</family>
 </webscript>
