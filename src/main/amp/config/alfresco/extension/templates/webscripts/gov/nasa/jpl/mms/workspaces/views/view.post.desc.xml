<webscript>
	<shortname>post view with {viewId}</shortname>
    <description>
    	post view with id, {viewId}, and add to the parent view with id, {parent} 
	</description>
  	<url>/workspaces/{workspaceId}/views/{viewId}?parent={parent}</url>
  	<format default="json">argument</format>
  	<authentication>user</authentication>
  	<transaction>none</transaction>
  	<family>MMS REST API</family>
 </webscript>
