<webscript>
	<shortname>delete snapshot with {snapshotId}</shortname>
    <description>
    	delete snapshot with {snapshotId}
	</description>
  	<url>/workspaces/{workspaceId}/sites/{siteId}/configurations/{configurationId}/snapshots/{snapshotId}</url>
  	<format default="json">any</format>
  	<authentication>user</authentication>
  	<transaction allow="readwrite">required</transaction>
  	<family>MMS REST API</family>
 </webscript>