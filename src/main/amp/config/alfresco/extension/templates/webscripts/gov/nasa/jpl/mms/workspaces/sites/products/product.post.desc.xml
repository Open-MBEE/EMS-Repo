<webscript>
	<shortname>post product with {productId}</shortname>
    <description>
    	post product with {productId}
	</description>
  	<url>/workspaces/{workspaceId}/sites/{siteId}/products/{productId}</url>
  	<format default="json">any</format>
  	<authentication>user</authentication>
  	<transaction allow="readwrite">required</transaction>
  	<family>MMS REST API</family>
 </webscript>