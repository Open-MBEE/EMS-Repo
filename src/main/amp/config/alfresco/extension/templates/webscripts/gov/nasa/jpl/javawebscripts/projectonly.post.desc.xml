<webscript> 
  <shortname>Update Project with version</shortname> 
  <description>
  </description>
  <url>/javawebscripts/projects/{projectId}?fix={fix?}</url>
  <format default="json">any</format> 
  <authentication>user</authentication> 
  <transaction allow="readwrite">required</transaction>
  <family>EMS Java Webscripts</family>
  <args>
  	<arg>
  		<name>posted content</name>
  		<description>The request body of the post as json:
  		{
  			"name": magicdraw-project-name,
  			"projectVersion": magicdraw-project-version (if available)
  		}
  		</description>
  	</arg>
  	<arg>
  		<name>fix</name>
  		<description>Force the fix (if true) if there are any discrepancies (e.g., project exists, but has different name).</description>
  	</arg>
  </args>
</webscript>